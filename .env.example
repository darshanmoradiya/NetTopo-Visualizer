# Example environment variables for NetTopo-Visualizer (Vite)
# Provide one or more comma-separated URLs (relative or absolute) to fetch topology JSON from.
# If not provided, the app tries `/data/raw_data_complete.json` then `/raw_data_complete.json`.

# Single URL example:
VITE_TOPOLOGY_URL=/data/raw_data_complete.json

# Or multiple comma-separated fallbacks (tried left-to-right):
# VITE_TOPOLOGY_URLS=/data/raw_data_complete.json,/raw_data_complete.json

# Poll interval in milliseconds (default 5000)
VITE_POLL_INTERVAL_MS=5000
